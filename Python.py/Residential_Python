Residential_Controller

Number_of_floors = 10
Number_of_elevators = 2

class ElevatorController:
    buttons = []

    def _init_ (self):
        self.number_of_floor = number_of_floors
        self.number_of_elevator = number_of_elevators

    def setButton (floorNumber, Direction):
        b = findButton(floorNumber, Direction)
        b.status = "active"

    def findbutton(floorNumber, direction):
        return Button()

    def requestElevator (self, floorNumber, direction):
        setButton (floorNumber, direction)
        print("floorNumber="+str(FloorNumber))
        print("direction="+str(Direction))

    def requestFloor (self, Elevator, RequestedFloor):
        print("RequestedFloor")
        print("floorNumber="+ str(FloorNumber))
        print("Direction="+ str(Direction))

    def setButton (floorNumber, Direction):
        b = findButton(floorNumber, Direction)
        b.status = "Inactive"

        controller.ElevatorControllers(10,2)
        controller.requestElevator(2, “UP”)
        controller.requestFloor(2, “UP”)
